diff --git a/netwerk/protocol/http/HttpBaseChannel.cpp b/netwerk/protocol/http/HttpBaseChannel.cpp
index b84597f12a..416c06f7ed 100644
--- a/netwerk/protocol/http/HttpBaseChannel.cpp
+++ b/netwerk/protocol/http/HttpBaseChannel.cpp
@@ -114,6 +114,8 @@
 #include "mozilla/dom/ContentChild.h"
 #include "nsQueryObject.h"
 
+#include "nsHttpHandler.h"
+
 using mozilla::dom::ForceMediaDocument;
 using mozilla::dom::RequestMode;
 
@@ -864,6 +866,9 @@ HttpBaseChannel::SetUploadStream(nsIInputStream* stream,
   // and so we select POST as the request method if contentType and
   // contentLength are unspecified.
 
+  nsIURI* uri = mURI;
+  printf("Hello %s\n", uri->GetSpecOrDefault().get());
+
   if (stream) {
     nsAutoCString method;
     bool hasHeaders = false;
@@ -1240,6 +1245,10 @@ HttpBaseChannel::ExplicitSetUploadStream(nsIInputStream* aStream,
   if (mURI) {
       mURI->GetSpec(uriSpec);
   }
+    
+  // } else {
+
+  // }
 
   LOG(("ANALYTICS: HttpBaseChannel::ExplicitSetUploadStream completed in %.3f ms [this=%p] [channelId=%llu] URL=%s\n",
        milliseconds, this, mChannelId, uriSpec.get()));
diff --git a/netwerk/protocol/http/HttpBaseChannel.h b/netwerk/protocol/http/HttpBaseChannel.h
index 622302b14a..1d3e404134 100644
--- a/netwerk/protocol/http/HttpBaseChannel.h
+++ b/netwerk/protocol/http/HttpBaseChannel.h
@@ -683,6 +683,17 @@ class HttpBaseChannel : public nsHashPropertyBag,
     StoreRequestObserversCalled(true);
   }
 
+  inline void CallReplaceNonce(Credential cred, nsACString& url) {
+    gHttpHandler->ReplaceNonce(this, cred, url);
+    // printf("HttpBaseChannel::CallReplaceNonce called\n");
+  }
+
+  inline mozilla::net::Credential CallGetCredential(nsACString& url) {
+    mozilla::net::Credential cred = gHttpHandler->GetCredential(url);
+    // printf("HttpBaseChannel::CallGetCredential called\n");
+    return cred;
+  }
+
   // Helper function to simplify getting notification callbacks.
   template <class T>
   void GetCallback(nsCOMPtr<T>& aResult) {
diff --git a/netwerk/protocol/http/moz.build b/netwerk/protocol/http/moz.build
index 94cfc87914..55fed0e6f7 100644
--- a/netwerk/protocol/http/moz.build
+++ b/netwerk/protocol/http/moz.build
@@ -31,6 +31,7 @@ XPIDL_SOURCES += [
     "nsIRaceCacheWithNetwork.idl",
     "nsIReplacedHttpResponse.idl",
     "nsITlsHandshakeListener.idl",
+    "nsIWebRequestConfig.idl",
     "nsIWellKnownOpportunisticUtils.idl",
 ]
 
diff --git a/netwerk/protocol/http/nsHttpChannel.cpp b/netwerk/protocol/http/nsHttpChannel.cpp
index 75aa9c07b2..bcacfec16d 100644
--- a/netwerk/protocol/http/nsHttpChannel.cpp
+++ b/netwerk/protocol/http/nsHttpChannel.cpp
@@ -184,6 +184,9 @@ namespace {
     ((mFirstResponseSource == RESPONSE_FROM_NETWORK) && \
      ((req) != mTransactionPump))))
 
+#define MYLOG(args) MOZ_LOG(gMyLog, mozilla::LogLevel::Debug, args)
+static mozilla::LazyLogModule gMyLog("my-on-modify-request");
+
 static NS_DEFINE_CID(kStreamListenerTeeCID, NS_STREAMLISTENERTEE_CID);
 
 enum ChannelDisposition {
@@ -824,6 +827,18 @@ nsresult nsHttpChannel::ContinuePrepareToConnect() {
   // notify "http-on-modify-request" observers
   CallOnModifyRequestObservers();
 
+  // TODO: I think the call to replace the nonce is fine here
+  nsCString url = mURI->GetSpecOrDefault();
+  // TODO: Error checking
+  // printf("nsHttpChannel::ContinuePrepareToConnect got URI: %s\n", url.get());
+  mozilla::net::Credential cred = CallGetCredential(url);
+  if (cred.IsEmpty()) {
+    // printf("nsHttpChannel::ContinuePrepareToConnect no credentials\n");
+  } else {
+    printf("nsHttpChannel::ContinuePrepareToConnect got credentials\n");
+    CallReplaceNonce(cred, url);
+  }
+
   return CallOrWaitForResume(
       [](auto* self) { return self->OnBeforeConnect(); });
 }
@@ -9151,6 +9166,7 @@ nsHttpChannel::OnStartRequest(nsIRequest* request) {
           nsCOMPtr<nsIObserverService> obsService =
               services::GetObserverService();
           if (obsService)
+            // MYLOG(("nsHttpChannel: Notifying observers in nsHttpChannel"));
             obsService->NotifyObservers(static_cast<nsIHttpChannel*>(this),
                                         "httpchannel-fallback", nullptr);
           return NS_OK;
diff --git a/netwerk/protocol/http/nsHttpHandler.cpp b/netwerk/protocol/http/nsHttpHandler.cpp
index 0fc582d28e..34d14d3cc7 100644
--- a/netwerk/protocol/http/nsHttpHandler.cpp
+++ b/netwerk/protocol/http/nsHttpHandler.cpp
@@ -18,6 +18,7 @@
 #include "nsHTTPCompressConv.h"
 #include "nsHttpAuthCache.h"
 #include "nsStandardURL.h"
+#include "nsString.h"
 #include "LoadContextInfo.h"
 #include "nsCategoryManagerUtils.h"
 #include "nsDirectoryServiceDefs.h"
@@ -93,6 +94,16 @@
 
 #include <bitset>
 
+#include "jsapi.h"
+#include "js/CompilationAndEvaluation.h"
+#include "js/SourceText.h"
+#include "mozilla/dom/ScriptSettings.h"
+#include "js/JSON.h"
+#include "js/RootingAPI.h"
+
+#include <nsStringStream.h>
+#include "nsNetUtil.h"
+
 #if defined(XP_UNIX)
 #  include <sys/utsname.h>
 #endif
@@ -133,6 +144,8 @@
 #define UA_PREF(_pref) UA_PREF_PREFIX _pref
 #define HTTP_PREF(_pref) HTTP_PREF_PREFIX _pref
 #define BROWSER_PREF(_pref) BROWSER_PREF_PREFIX _pref
+#define MYLOG(args) MOZ_LOG(gMyLog, mozilla::LogLevel::Debug, args)
+
 
 #define NS_HTTP_PROTOCOL_FLAGS \
   (URI_STD | ALLOWS_PROXY | ALLOWS_PROXY_HTTP | URI_LOADABLE_BY_ANYONE)
@@ -145,6 +158,8 @@ namespace mozilla::net {
 
 LazyLogModule gHttpLog("nsHttp");
 LazyLogModule gHttpIOLog("HttpIO");
+static mozilla::LazyLogModule gMyLog("my-on-modify-request");
+
 
 #ifdef ANDROID
 static nsCString GetDeviceModelId() {
@@ -882,8 +897,9 @@ nsresult nsHttpHandler::GetIOService(nsIIOService** result) {
 }
 
 void nsHttpHandler::NotifyObservers(nsIChannel* chan, const char* event) {
-  LOG(("nsHttpHandler::NotifyObservers [this=%p chan=%p event=\"%s\"]\n", this,
-       chan, event));
+  // MYLOG(("Before on-modify-request\n"));
+  // MYLOG(("nsHttpHandler::NotifyObservers [this=%p chan=%p event=\"%s\"]\n", this,
+     //  chan, event));
   nsCOMPtr<nsIObserverService> obsService = services::GetObserverService();
   if (obsService) obsService->NotifyObservers(chan, event, nullptr);
 }
@@ -2123,7 +2139,7 @@ nsresult nsHttpHandler::SetAcceptEncodings(const char* aAcceptEncodings,
 
 NS_IMPL_ISUPPORTS(nsHttpHandler, nsIHttpProtocolHandler,
                   nsIProxiedProtocolHandler, nsIProtocolHandler, nsIObserver,
-                  nsISupportsWeakReference, nsISpeculativeConnect)
+                  nsISupportsWeakReference, nsISpeculativeConnect, nsIWebRequestConfig)
 
 //-----------------------------------------------------------------------------
 // nsHttpHandler::nsIProtocolHandler
@@ -2186,6 +2202,13 @@ nsresult nsHttpHandler::SetupChannelInternal(
   return NS_OK;
 }
 
+void PrintHex(const char* data, size_t len) {
+  for (size_t i = 0; i < len; i++) {
+    printf("%02X ", static_cast<unsigned char>(data[i]));
+  }
+  printf("\n");
+}
+
 NS_IMETHODIMP
 nsHttpHandler::NewProxiedChannel(nsIURI* uri, nsIProxyInfo* givenProxyInfo,
                                  uint32_t proxyResolveFlags, nsIURI* proxyURI,
@@ -2196,14 +2219,22 @@ nsHttpHandler::NewProxiedChannel(nsIURI* uri, nsIProxyInfo* givenProxyInfo,
 
   if (IsNeckoChild()) {
     httpChannel = new HttpChannelChild();
+    
   } else {
+      // MYLOG(("Parent log: info %s", mDebugInfoFromWebRequest.get()));
     // HACK: make sure PSM gets initialized on the main thread.
     net_EnsurePSMInit();
     httpChannel = new nsHttpChannel();
   }
 
-  return SetupChannelInternal(httpChannel, uri, givenProxyInfo,
+  nsresult setup_result = SetupChannelInternal(httpChannel, uri, givenProxyInfo,
                               proxyResolveFlags, proxyURI, aLoadInfo, result);
+  if(NS_FAILED(setup_result)){
+    printf("SetupChannelInternal failed\n");
+    return setup_result;
+  }
+
+  return setup_result;
 }
 
 nsresult nsHttpHandler::CreateTRRServiceChannel(
@@ -2545,6 +2576,7 @@ nsresult nsHttpHandler::SpeculativeConnectInternal(
       nsPrintfCString debugHashKey("%s", ci->HashKey().get());
       obsService->NotifyObservers(nullptr, "speculative-connect-request",
                                   NS_ConvertUTF8toUTF16(debugHashKey).get());
+      MYLOG(("Notified in SpeculativeConnectInternal"));
       for (auto* cp :
            dom::ContentParent::AllProcesses(dom::ContentParent::eLive)) {
         PNeckoParent* neckoParent =
@@ -2571,6 +2603,47 @@ nsHttpHandler::SpeculativeConnect(nsIURI* aURI, nsIPrincipal* aPrincipal,
                                     aAnonymous);
 }
 
+NS_IMETHODIMP
+nsHttpHandler::AddCredential(const nsACString& url, const nsACString& nonce, const nsACString& actualCredential)
+{
+  Credential cred;
+  cred.nonce = nonce;
+  cred.actualCredential = actualCredential;
+  nsCOMPtr<nsIURI> uri;
+  NS_NewURI(getter_AddRefs(uri), url);
+  // nsCString formated_url;
+  // uri->GetSpec(formated_url);
+  nsCString host;
+  uri->GetAsciiHost(host);
+  // printf("ASCII HOST: %s\n", host.get());
+  // formated_url.Trim(" \t\r\n");
+  if (auto entry = mCredMap.Lookup(host)) {
+    const Credential& cred = entry.Data();
+    printf("Credential already found in map:\n"
+      "  nonce=%s\n"
+      "  actualCredential=%s\n",
+      cred.nonce.get(),
+      cred.actualCredential.get());
+      } else {
+        printf("No credential found for key, adding to map."); // =%s\n", url));
+        }
+        mCredMap.InsertOrUpdate(host, std::move(cred));
+        printf("!!!!!!!!!!!!!!Set debug info!!!!!!!!!!!!!!!!!!!!!!");
+        // const nsACString aKey = url;
+        if (auto entry = mCredMap.Lookup(host)) {
+          const Credential& cred = entry.Data();
+          printf("Credential found:\n"
+            "  nonce=%s\n"
+            "  actualCredential=%s\n",
+            cred.nonce.get(),
+            cred.actualCredential.get());
+            } else {
+              printf("No credential found for key"); // =%s\n", url));
+            }
+          printf("Finished setting credential.");
+  return NS_OK;
+}
+
 NS_IMETHODIMP nsHttpHandler::SpeculativeConnectWithOriginAttributes(
     nsIURI* aURI, JS::Handle<JS::Value> aOriginAttributes,
     nsIInterfaceRequestor* aCallbacks, bool aAnonymous, JSContext* aCx) {
@@ -3101,4 +3174,117 @@ void nsHttpHandler::ObserveHttpActivityWithArgs(
       aExtraStringData);
 }
 
+  Credential nsHttpHandler::GetCredential(const nsACString& url){
+    nsCOMPtr<nsIURI> uri;
+    NS_NewURI(getter_AddRefs(uri), url);
+    nsCString host;
+    uri->GetAsciiHost(host);
+    // printf("Getting credential for url: %s\n", host.get());
+    // printf("ASCII HOST: %s\n", host.get());
+    auto entry = mCredMap.Lookup(host);
+    if (entry) {
+      const Credential& cred = entry.Data();
+      printf("Credential found:\n"
+        "  nonce=%s\n"
+        "  actualCredential=%s\n",
+        cred.nonce.get(),
+        cred.actualCredential.get());
+        return cred;
+      } else {
+        // printf("No credential found for key\n"); // %s\n", url);
+        return Credential();
+      }
+    }
+
+  void nsHttpHandler::RemoveCredential(const nsACString& url){
+    nsCOMPtr<nsIURI> uri;
+    NS_NewURI(getter_AddRefs(uri), url);
+    nsCString host;
+    uri->GetAsciiHost(host);
+    mCredMap.Remove(host);
+    }
+
+  // The return for this function likely needs to be something else
+  //  if there is an error then the calling function needs to handle it properly 
+  nsresult nsHttpHandler::ReplaceNonce(nsIHttpChannel* chan, Credential cred, nsACString& url){
+  printf("ReplaceNonce: Starting\n");
+  
+  nsCOMPtr<nsIUploadChannel> uploadChannel = do_QueryInterface(chan);
+  nsCOMPtr<nsIInputStream> checkStream;
+  if (!uploadChannel || NS_FAILED(uploadChannel->GetUploadStream(getter_AddRefs(checkStream))) || !checkStream) {
+    printf("ReplaceNonce: No upload stream found\n");
+    return NS_OK;
+  }
+
+  // Save original Content-Type
+  nsAutoCString contentType;
+  nsresult rv = chan->GetRequestHeader("Content-Type"_ns, contentType);
+  if (NS_FAILED(rv)) {
+    printf("ReplaceNonce: No Content-Type header found\n");
+    contentType.Truncate();
+  } else {
+    printf("ReplaceNonce: Original Content-Type: %s\n", contentType.get());
+  }
+
+  // Read stream
+  nsCOMPtr<nsIInputStream> stream;
+  uploadChannel->GetUploadStream(getter_AddRefs(stream));
+  nsCString body;
+  rv = NS_ReadInputStreamToString(stream, body, -1);
+  if (NS_FAILED(rv)) {
+    printf("ReplaceNonce: Failed to read stream, rv=0x%x\n", static_cast<uint32_t>(rv));
+    return rv;
+  }
+  printf("ReplaceNonce: Original body (%zu bytes): %s\n", body.Length(), body.get());
+
+  // Replace nonce
+  int32_t pos = body.Find(cred.nonce);
+  if (pos < 0) {
+    printf("ReplaceNonce: Nonce not found in body\n");
+  } else {
+  printf("ReplaceNonce: Found nonce at position %d\n", pos);
+  
+  body.ReplaceSubstring(cred.nonce, cred.actualCredential);
+  printf("ReplaceNonce: Modified body (%zu bytes): %s\n", body.Length(), body.get());
+  RemoveCredential(url);
+  }
+
+  // Create new stream and replace
+  nsCOMPtr<nsIInputStream> newStream;
+  rv = NS_NewByteInputStream(getter_AddRefs(newStream), body, NS_ASSIGNMENT_COPY);
+  if (NS_FAILED(rv)) {
+    printf("ReplaceNonce: Failed to create new stream, rv=0x%x\n", static_cast<uint32_t>(rv));
+    return rv;
+  }
+
+  nsCOMPtr<nsIUploadChannel2> uploadChannel2 = do_QueryInterface(chan);
+  if (uploadChannel2) {
+    printf("ReplaceNonce: Using nsIUploadChannel2\n");
+    nsAutoCString method;
+    rv = chan->GetRequestMethod(method);
+    if (NS_FAILED(rv)) {
+      printf("ReplaceNonce: Failed to get request method, rv=0x%x\n", static_cast<uint32_t>(rv));
+      return rv; // Not sure what to do here. This will kill the request
+    }
+    printf("ReplaceNonce: Request method: %s\n", method.get());
+    rv = uploadChannel2->ExplicitSetUploadStream(newStream, contentType, body.Length(), method, false);
+  } else {
+    printf("ReplaceNonce: Falling back to nsIUploadChannel\n");
+    rv = uploadChannel->SetUploadStream(newStream, contentType, body.Length());
+  }
+  
+  if (NS_FAILED(rv)) {
+    printf("ReplaceNonce: Failed to set upload stream, rv=0x%x\n", static_cast<uint32_t>(rv));
+    return rv;
+  }
+
+  rv = chan->SetRequestHeader("Content-Length"_ns, nsPrintfCString("%zu", body.Length()), false);
+  if (NS_FAILED(rv)) {
+    printf("ReplaceNonce: Failed to set Content-Length header, rv=0x%x\n", static_cast<uint32_t>(rv));
+  }
+  printf("ReplaceNonce: Successfully replaced nonce and updated Content-Length to %zu\n", body.Length());
+  
+  return rv;
+}
+
 }  // namespace mozilla::net
diff --git a/netwerk/protocol/http/nsHttpHandler.h b/netwerk/protocol/http/nsHttpHandler.h
index bb82c46edf..dcf8fa836e 100644
--- a/netwerk/protocol/http/nsHttpHandler.h
+++ b/netwerk/protocol/http/nsHttpHandler.h
@@ -29,6 +29,8 @@
 #include "nsTHashMap.h"
 #include "nsTHashSet.h"
 
+#include "nsIWebRequestConfig.h"
+
 #ifdef DEBUG
 #  include "nsIOService.h"
 #endif
@@ -99,6 +101,14 @@ enum FrameCheckLevel {
   FRAMECHECK_STRICT
 };
 
+struct Credential {
+  nsCString nonce;
+  nsCString actualCredential;
+  bool IsEmpty() const {
+    return nonce.IsEmpty() || actualCredential.IsEmpty();
+  }
+};
+
 //-----------------------------------------------------------------------------
 // nsHttpHandler - protocol handler for HTTP and HTTPS
 //-----------------------------------------------------------------------------
@@ -106,7 +116,8 @@ enum FrameCheckLevel {
 class nsHttpHandler final : public nsIHttpProtocolHandler,
                             public nsIObserver,
                             public nsSupportsWeakReference,
-                            public nsISpeculativeConnect {
+                            public nsISpeculativeConnect,
+                            public nsIWebRequestConfig {
  public:
   NS_DECL_THREADSAFE_ISUPPORTS
   NS_DECL_NSIPROTOCOLHANDLER
@@ -117,6 +128,14 @@ class nsHttpHandler final : public nsIHttpProtocolHandler,
 
   static already_AddRefed<nsHttpHandler> GetInstance();
 
+  NS_IMETHOD AddCredential(const nsACString& url, const nsACString& nonce, const nsACString& actualCredential) override;
+
+  nsresult ReplaceNonce(nsIHttpChannel* chan, Credential cred, nsACString& url);
+
+  Credential GetCredential(const nsACString& url);
+
+  void RemoveCredential(const nsACString& url);
+
   [[nodiscard]] nsresult AddAcceptAndDictionaryHeaders(
       nsIURI* aURI, ExtContentPolicyType aType, nsHttpRequestHead* aRequest,
       bool aSecure, nsHttpChannel* aChan, void (*aSuspend)(nsHttpChannel*),
@@ -523,6 +542,10 @@ void OnRequestCredentials(nsIHttpChannel* chan) {
 
   [[nodiscard]] nsresult Init();
 
+ public:
+  nsTHashMap<nsCStringHashKey, Credential> mCredMap; 
+
+ private:
   //
   // Useragent/prefs helper methods
   //
diff --git a/netwerk/protocol/http/nsIWebRequestConfig.idl b/netwerk/protocol/http/nsIWebRequestConfig.idl
new file mode 100644
index 0000000000..c857a324f3
--- /dev/null
+++ b/netwerk/protocol/http/nsIWebRequestConfig.idl
@@ -0,0 +1,13 @@
+#include "nsISupports.idl"
+
+[scriptable, uuid(6bbacd65-9a7d-4441-aedc-ad3c9b7a17f3)]
+interface nsIWebRequestConfig : nsISupports
+{
+  /**
+   * Set debug/config information that nsHttpHandler will consult when
+   * creating new HTTP channels.
+   *
+   * This is process-global state owned by nsHttpHandler.
+   */
+  void AddCredential(in ACString url, in ACString nonce, in ACString actualCredential);
+};
\ No newline at end of file
diff --git a/toolkit/components/extensions/parent/ext-webRequest.js b/toolkit/components/extensions/parent/ext-webRequest.js
index f5d4199ead..fa25102586 100644
--- a/toolkit/components/extensions/parent/ext-webRequest.js
+++ b/toolkit/components/extensions/parent/ext-webRequest.js
@@ -4,10 +4,15 @@
 
 "use strict";
 
+// eslint-disable-next-line mozilla/reject-importGlobalProperties
+
 ChromeUtils.defineESModuleGetters(this, {
   WebRequest: "resource://gre/modules/WebRequest.sys.mjs",
 });
 
+const uuidGenerator = Components.classes["@mozilla.org/uuid-generator;1"]
+  .getService(Components.interfaces.nsIUUIDGenerator);
+
 // Add this debug block temporarily:
 console.log("=== WebRequest Debug ===");
 // console.log("WebRequest object:", WebRequest);
@@ -16,6 +21,13 @@ console.log("=== WebRequest Debug ===");
 // console.log("Available WebRequest keys:", Object.keys(WebRequest));
 console.log("========================");
 
+console.log("Here")
+
+const { classes: Cc, interfaces: Ci } = Components;
+          const config = Cc["@mozilla.org/network/protocol;1?name=http"]
+                        .getService(Ci.nsIWebRequestConfig);
+          console.log("Got config", config);
+
 var { parseMatchPatterns, DefaultMap } = ExtensionUtils;
 
 const MAX_HANDLER_BEHAVIOR_CHANGED_CALLS_PER_10_MINUTES = 20;
@@ -257,6 +269,17 @@ this.webRequest = class extends ExtensionAPIPersistent {
 
           ChromeUtils.clearResourceCache({ target: "content" });
         },
+        handleCredentialReplacement: async (actualCredential, url) => {
+          console.log("Received credentials: ", actualCredential);
+          url = url.trim();
+          console.log("Received url: ", url);
+          let nonce = uuidGenerator.generateUUID().toString().slice(1, -1);
+          console.log("Generated Nonce: ", nonce);
+          console.log("Calling network method...");
+          config.AddCredential(url, nonce, actualCredential);
+          console.log("After AddCredentials");
+          return nonce;
+        }
       },
     };
   }
diff --git a/toolkit/components/extensions/schemas/web_request.json b/toolkit/components/extensions/schemas/web_request.json
index 8aea389e3f..bebcd0ce91 100644
--- a/toolkit/components/extensions/schemas/web_request.json
+++ b/toolkit/components/extensions/schemas/web_request.json
@@ -456,6 +456,25 @@
           }
         ]
       },
+      {
+        "name": "handleCredentialReplacement",
+        "type": "function",
+        "description": "test endpoint",
+        "async": true,
+        "parameters": [
+          {
+            "type": "string",
+            "name": "actualCredential"
+          },
+          {
+            "type": "string",
+            "name": "url"
+          }
+        ],
+        "returns": {
+          "type": "string"
+        }
+      },
       {
         "name": "filterResponseData",
         "permissions": ["webRequestBlocking"],
