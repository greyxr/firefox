diff --git a/dom/base/nsGlobalWindowInner.cpp b/dom/base/nsGlobalWindowInner.cpp
index 4dbaac638f2c..b4c84ab14447 100644
--- a/dom/base/nsGlobalWindowInner.cpp
+++ b/dom/base/nsGlobalWindowInner.cpp
@@ -112,6 +112,7 @@
 #include "mozilla/dom/ContentMediaController.h"
 #include "mozilla/dom/CookieStore.h"
 #include "mozilla/dom/Credential.h"
+#include "mozilla/dom/Secrets.h"
 #include "mozilla/dom/CustomElementRegistry.h"
 #include "mozilla/dom/DebuggerNotification.h"
 #include "mozilla/dom/DebuggerNotificationBinding.h"
@@ -1274,6 +1275,7 @@ void nsGlobalWindowInner::FreeInnerObjects() {
 
   mConsole = nullptr;
   mCookieStore = nullptr;
+  mSecrets = nullptr;
   mDocumentPiP = nullptr;
 
   mPaintWorklet = nullptr;
@@ -1470,6 +1472,7 @@ NS_IMPL_CYCLE_COLLECTION_TRAVERSE_BEGIN_INTERNAL(nsGlobalWindowInner)
   NS_IMPL_CYCLE_COLLECTION_TRAVERSE(mCrypto)
   NS_IMPL_CYCLE_COLLECTION_TRAVERSE(mConsole)
   NS_IMPL_CYCLE_COLLECTION_TRAVERSE(mCookieStore)
+  NS_IMPL_CYCLE_COLLECTION_TRAVERSE(mSecrets)
   NS_IMPL_CYCLE_COLLECTION_TRAVERSE(mDocumentPiP)
   NS_IMPL_CYCLE_COLLECTION_TRAVERSE(mPaintWorklet)
   NS_IMPL_CYCLE_COLLECTION_TRAVERSE(mExternal)
@@ -1587,6 +1590,7 @@ NS_IMPL_CYCLE_COLLECTION_UNLINK_BEGIN(nsGlobalWindowInner)
   NS_IMPL_CYCLE_COLLECTION_UNLINK(mCrypto)
   NS_IMPL_CYCLE_COLLECTION_UNLINK(mConsole)
   NS_IMPL_CYCLE_COLLECTION_UNLINK(mCookieStore)
+  NS_IMPL_CYCLE_COLLECTION_UNLINK(mSecrets)
   NS_IMPL_CYCLE_COLLECTION_UNLINK(mPaintWorklet)
   NS_IMPL_CYCLE_COLLECTION_UNLINK(mExternal)
   NS_IMPL_CYCLE_COLLECTION_UNLINK(mIntlUtils)
@@ -7406,6 +7410,14 @@ already_AddRefed<CookieStore> nsGlobalWindowInner::CookieStore() {
   return do_AddRef(mCookieStore);
 }
 
+already_AddRefed<dom::Secrets> nsGlobalWindowInner::GetSecrets(
+    ErrorResult& aRv) {
+  if (!mSecrets) {
+    mSecrets = new dom::Secrets(this);
+  }
+  return do_AddRef(mSecrets);
+}
+
 DocumentPictureInPicture* nsGlobalWindowInner::DocumentPictureInPicture() {
   if (!mDocumentPiP) {
     mDocumentPiP = MakeRefPtr<class DocumentPictureInPicture>(this);
diff --git a/dom/base/nsGlobalWindowInner.h b/dom/base/nsGlobalWindowInner.h
index 9aa899cdd51e..ce47fae613d3 100644
--- a/dom/base/nsGlobalWindowInner.h
+++ b/dom/base/nsGlobalWindowInner.h
@@ -107,6 +107,7 @@ class ClientSource;
 class Console;
 class CookieStore;
 class Crypto;
+class Secrets;
 class CustomElementRegistry;
 class DataTransfer;
 class DocGroup;
@@ -669,6 +670,8 @@ class nsGlobalWindowInner final : public mozilla::dom::EventTarget,
 
   already_AddRefed<mozilla::dom::CookieStore> CookieStore();
 
+  already_AddRefed<mozilla::dom::Secrets> GetSecrets(mozilla::ErrorResult& aRv);
+
   mozilla::dom::DocumentPictureInPicture* GetExtantDocumentPictureInPicture()
       override {
     return mDocumentPiP;
@@ -1401,6 +1404,7 @@ class nsGlobalWindowInner final : public mozilla::dom::EventTarget,
   RefPtr<mozilla::dom::cache::CacheStorage> mCacheStorage;
   RefPtr<mozilla::dom::Console> mConsole;
   RefPtr<mozilla::dom::CookieStore> mCookieStore;
+  RefPtr<mozilla::dom::Secrets> mSecrets;
   RefPtr<mozilla::dom::DocumentPictureInPicture> mDocumentPiP;
   RefPtr<mozilla::dom::Worklet> mPaintWorklet;
   RefPtr<mozilla::dom::External> mExternal;
diff --git a/dom/credentialmanagement/Secrets.cpp b/dom/credentialmanagement/Secrets.cpp
new file mode 100644
index 000000000000..3d440932f7cd
--- /dev/null
+++ b/dom/credentialmanagement/Secrets.cpp
@@ -0,0 +1,71 @@
+#include "mozilla/dom/Secrets.h"
+#include "mozilla/dom/SecretsBinding.h"
+#include "mozilla/dom/BrowsingContext.h"
+#include "mozilla/net/NeckoChild.h"
+#include "nsIURI.h"
+
+namespace mozilla::dom {
+
+NS_IMPL_CYCLE_COLLECTION_WRAPPERCACHE(Secrets, mParent)
+NS_IMPL_CYCLE_COLLECTING_ADDREF(Secrets)
+NS_IMPL_CYCLE_COLLECTING_RELEASE(Secrets)
+NS_INTERFACE_MAP_BEGIN_CYCLE_COLLECTION(Secrets)
+  NS_WRAPPERCACHE_INTERFACE_MAP_ENTRY
+  NS_INTERFACE_MAP_ENTRY(nsISupports)
+NS_INTERFACE_MAP_END
+
+Secrets::Secrets(nsPIDOMWindowInner* aParent) : mParent(aParent) {}
+
+JSObject* Secrets::WrapObject(JSContext* aCx,
+                              JS::Handle<JSObject*> aGivenProto) {
+  return Secrets_Binding::Wrap(aCx, this, aGivenProto);
+}
+
+/* static */
+bool Secrets::IsEnabled(JSContext* aCx, JSObject* aGlobal) { return true; }
+
+void Secrets::RegisterNonce(const nsAString& aNonce, const nsAString& aSecret,
+                            const nsTArray<nsString>& aMethods,
+                            ErrorResult& aRv) {
+  BrowsingContext* bc = mParent->GetBrowsingContext();
+  if (!bc) {
+    aRv.Throw(NS_ERROR_FAILURE);
+    return;
+  }
+
+  nsCOMPtr<nsIURI> docURI = mParent->GetDocumentURI();
+  if (!docURI) {
+    aRv.Throw(NS_ERROR_FAILURE);
+    return;
+  }
+
+  nsAutoCString scheme;
+  nsAutoCString host;
+  int32_t port = -1;
+  docURI->GetScheme(scheme);
+  docURI->GetAsciiHost(host);
+  docURI->GetPort(&port);
+  nsAutoCString origin;
+  origin.Assign(scheme);
+  origin.AppendLiteral("://");
+  origin.Append(host);
+  if (port != -1) {
+    origin.Append(':');
+    origin.AppendInt(port);
+  }
+
+  nsTArray<nsCString> methods;
+  for (const auto& m : aMethods) {
+    methods.AppendElement(NS_ConvertUTF16toUTF8(m));
+  }
+
+  if (!mozilla::net::gNeckoChild) {
+    aRv.Throw(NS_ERROR_NOT_INITIALIZED);
+    return;
+  }
+  mozilla::net::gNeckoChild->SendAddCredential(
+      bc->Id(), NS_ConvertUTF16toUTF8(aNonce), NS_ConvertUTF16toUTF8(aSecret),
+      methods, origin);
+}
+
+}  // namespace mozilla::dom
diff --git a/dom/credentialmanagement/Secrets.h b/dom/credentialmanagement/Secrets.h
new file mode 100644
index 000000000000..d3dffa251d5f
--- /dev/null
+++ b/dom/credentialmanagement/Secrets.h
@@ -0,0 +1,32 @@
+#ifndef mozilla_dom_Secrets_h
+#define mozilla_dom_Secrets_h
+
+#include "nsCycleCollectionParticipant.h"
+#include "nsPIDOMWindow.h"
+#include "nsWrapperCache.h"
+
+namespace mozilla::dom {
+
+class Secrets final : public nsISupports, public nsWrapperCache {
+ public:
+  NS_DECL_CYCLE_COLLECTING_ISUPPORTS
+  NS_DECL_CYCLE_COLLECTION_WRAPPERCACHE_CLASS(Secrets)
+
+  explicit Secrets(nsPIDOMWindowInner* aParent);
+
+  nsISupports* GetParentObject() const { return mParent; }
+  JSObject* WrapObject(JSContext* aCx,
+                       JS::Handle<JSObject*> aGivenProto) override;
+
+  static bool IsEnabled(JSContext* aCx, JSObject* aGlobal);
+
+  void RegisterNonce(const nsAString& aNonce, const nsAString& aSecret,
+                     const nsTArray<nsString>& aMethods, ErrorResult& aRv);
+
+ private:
+  ~Secrets() = default;
+  nsCOMPtr<nsPIDOMWindowInner> mParent;
+};
+
+}  // namespace mozilla::dom
+#endif
diff --git a/dom/credentialmanagement/moz.build b/dom/credentialmanagement/moz.build
index 17f02573d4b0..2ea35749b8c9 100644
--- a/dom/credentialmanagement/moz.build
+++ b/dom/credentialmanagement/moz.build
@@ -12,11 +12,13 @@ DIRS += ["identity"]
 EXPORTS.mozilla.dom += [
     "Credential.h",
     "CredentialsContainer.h",
+    "Secrets.h",
 ]
 
 UNIFIED_SOURCES += [
     "Credential.cpp",
     "CredentialsContainer.cpp",
+    "Secrets.cpp",
 ]
 
 include("/ipc/chromium/chromium-config.mozbuild")
diff --git a/dom/webidl/Secrets.webidl b/dom/webidl/Secrets.webidl
new file mode 100644
index 000000000000..dc491c9e7360
--- /dev/null
+++ b/dom/webidl/Secrets.webidl
@@ -0,0 +1,6 @@
+[Exposed=Window]
+interface Secrets {
+  [Throws]
+  undefined registerNonce(DOMString nonce, DOMString secret,
+                          optional sequence<DOMString> methods = []);
+};
diff --git a/dom/webidl/Window.webidl b/dom/webidl/Window.webidl
index 7d29889493b3..026d75aae87d 100644
--- a/dom/webidl/Window.webidl
+++ b/dom/webidl/Window.webidl
@@ -893,6 +893,12 @@ partial interface Window {
   [SameObject, Pref="dom.cookieStore.enabled"] readonly attribute CookieStore cookieStore;
 };
 
+partial interface Window {
+  [SameObject, Throws,
+   Func="mozilla::dom::Secrets::IsEnabled"]
+  readonly attribute Secrets secrets;
+};
+
 // https://html.spec.whatwg.org/multipage/browsers.html#origin-keyed-agent-clusters
 partial interface Window {
   [Pref="dom.origin_agent_cluster.enabled"] readonly attribute boolean originAgentCluster;
diff --git a/dom/webidl/moz.build b/dom/webidl/moz.build
index cf3df10e4fc2..027555d76e71 100644
--- a/dom/webidl/moz.build
+++ b/dom/webidl/moz.build
@@ -891,6 +891,7 @@ WEBIDL_FILES = [
     "ScreenOrientation.webidl",
     "ScriptProcessorNode.webidl",
     "ScrollAreaEvent.webidl",
+    "Secrets.webidl",
     "Selection.webidl",
     "ServiceWorker.webidl",
     "ServiceWorkerContainer.webidl",
diff --git a/netwerk/ipc/NeckoParent.cpp b/netwerk/ipc/NeckoParent.cpp
index 87f495d470ce..75a299750d7d 100644
--- a/netwerk/ipc/NeckoParent.cpp
+++ b/netwerk/ipc/NeckoParent.cpp
@@ -621,6 +621,16 @@ mozilla::ipc::IPCResult NeckoParent::RecvRemoveRequestContext(
   return IPC_OK();
 }
 
+mozilla::ipc::IPCResult NeckoParent::RecvAddCredential(
+    const uint64_t& bcID, const nsCString& nonce,
+    const nsCString& actualCredential, nsTArray<nsCString>&& methods,
+    const nsCString& origin) {
+  if (gHttpHandler) {
+    gHttpHandler->AddCredential(bcID, nonce, actualCredential, methods, origin);
+  }
+  return IPC_OK();
+}
+
 mozilla::ipc::IPCResult NeckoParent::RecvGetExtensionStream(
     nsIURI* aURI, GetExtensionStreamResolver&& aResolve) {
   if (!aURI) {
diff --git a/netwerk/ipc/NeckoParent.h b/netwerk/ipc/NeckoParent.h
index 0b0a0411500c..aacc07e9d2cd 100644
--- a/netwerk/ipc/NeckoParent.h
+++ b/netwerk/ipc/NeckoParent.h
@@ -178,6 +178,11 @@ class NeckoParent : public PNeckoParent {
   mozilla::ipc::IPCResult RecvRequestContextAfterDOMContentLoaded(
       const uint64_t& rcid);
   mozilla::ipc::IPCResult RecvRemoveRequestContext(const uint64_t& rcid);
+  mozilla::ipc::IPCResult RecvAddCredential(const uint64_t& bcID,
+                                             const nsCString& nonce,
+                                             const nsCString& actualCredential,
+                                             nsTArray<nsCString>&& methods,
+                                             const nsCString& origin);
 
   /* WebExtensions */
   mozilla::ipc::IPCResult RecvGetExtensionStream(
diff --git a/netwerk/ipc/PNecko.ipdl b/netwerk/ipc/PNecko.ipdl
index 61f4bb95c064..3659c3e208e0 100644
--- a/netwerk/ipc/PNecko.ipdl
+++ b/netwerk/ipc/PNecko.ipdl
@@ -129,6 +129,9 @@ parent:
   async RequestContextAfterDOMContentLoaded(uint64_t rcid);
   async RemoveRequestContext(uint64_t rcid);
 
+  async AddCredential(uint64_t bcID, nsCString nonce, nsCString actualCredential,
+                      nsCString[] methods, nsCString origin);
+
   async PCacheEntryWriteHandle(PHttpChannel channel);
 
   async PAltDataOutputStream(nsCString type, int64_t predictedSize, PHttpChannel? channel, PCacheEntryWriteHandle? handle);
diff --git a/netwerk/protocol/http/HttpBaseChannel.cpp b/netwerk/protocol/http/HttpBaseChannel.cpp
index b84597f12aef..bda403216ee3 100644
--- a/netwerk/protocol/http/HttpBaseChannel.cpp
+++ b/netwerk/protocol/http/HttpBaseChannel.cpp
@@ -114,6 +114,8 @@
 #include "mozilla/dom/ContentChild.h"
 #include "nsQueryObject.h"
 
+#include "nsHttpHandler.h"
+
 using mozilla::dom::ForceMediaDocument;
 using mozilla::dom::RequestMode;
 
diff --git a/netwerk/protocol/http/HttpBaseChannel.h b/netwerk/protocol/http/HttpBaseChannel.h
index 622302b14af2..d24e44afd6ac 100644
--- a/netwerk/protocol/http/HttpBaseChannel.h
+++ b/netwerk/protocol/http/HttpBaseChannel.h
@@ -683,6 +683,19 @@ class HttpBaseChannel : public nsHashPropertyBag,
     StoreRequestObserversCalled(true);
   }
 
+  inline nsresult CallReplaceNonce(const mozilla::net::Credential& cred,
+                                   uint64_t aBcID) {
+    return gHttpHandler->ReplaceNonce(this, cred, aBcID);
+  }
+
+  inline mozilla::net::Credential CallGetCredential(uint64_t aBcID) {
+    nsAutoCString uriSpec;
+    if (mURI) {
+      mURI->GetSpec(uriSpec);
+    }
+    return gHttpHandler->GetCredential(aBcID, uriSpec);
+  }
+
   // Helper function to simplify getting notification callbacks.
   template <class T>
   void GetCallback(nsCOMPtr<T>& aResult) {
diff --git a/netwerk/protocol/http/moz.build b/netwerk/protocol/http/moz.build
index 94cfc879144a..26a3ac668988 100644
--- a/netwerk/protocol/http/moz.build
+++ b/netwerk/protocol/http/moz.build
@@ -31,6 +31,7 @@ XPIDL_SOURCES += [
     "nsIRaceCacheWithNetwork.idl",
     "nsIReplacedHttpResponse.idl",
     "nsITlsHandshakeListener.idl",
+
     "nsIWellKnownOpportunisticUtils.idl",
 ]
 
diff --git a/netwerk/protocol/http/nsHttpChannel.cpp b/netwerk/protocol/http/nsHttpChannel.cpp
index 75aa9c07b267..6e5d94974f42 100644
--- a/netwerk/protocol/http/nsHttpChannel.cpp
+++ b/netwerk/protocol/http/nsHttpChannel.cpp
@@ -824,6 +824,14 @@ nsresult nsHttpChannel::ContinuePrepareToConnect() {
   // notify "http-on-modify-request" observers
   CallOnModifyRequestObservers();
 
+  if (mUploadStream) {
+    uint64_t bcID = mLoadInfo->GetBrowsingContextID();
+    mozilla::net::Credential cred = CallGetCredential(bcID);
+    if (!cred.IsEmpty()) {
+      CallReplaceNonce(cred, bcID);
+    }
+  }
+
   return CallOrWaitForResume(
       [](auto* self) { return self->OnBeforeConnect(); });
 }
diff --git a/netwerk/protocol/http/nsHttpHandler.cpp b/netwerk/protocol/http/nsHttpHandler.cpp
index 0fc582d28e06..86a15c67c9e4 100644
--- a/netwerk/protocol/http/nsHttpHandler.cpp
+++ b/netwerk/protocol/http/nsHttpHandler.cpp
@@ -7,6 +7,7 @@
 // HttpLog.h should generally be included first
 #include "HttpLog.h"
 
+#include <inttypes.h>
 #include "prsystem.h"
 
 #include "AltServiceChild.h"
@@ -18,6 +19,7 @@
 #include "nsHTTPCompressConv.h"
 #include "nsHttpAuthCache.h"
 #include "nsStandardURL.h"
+#include "nsString.h"
 #include "LoadContextInfo.h"
 #include "nsCategoryManagerUtils.h"
 #include "nsDirectoryServiceDefs.h"
@@ -93,6 +95,9 @@
 
 #include <bitset>
 
+#include <nsStringStream.h>
+#include "nsNetUtil.h"
+
 #if defined(XP_UNIX)
 #  include <sys/utsname.h>
 #endif
@@ -146,6 +151,7 @@ namespace mozilla::net {
 LazyLogModule gHttpLog("nsHttp");
 LazyLogModule gHttpIOLog("HttpIO");
 
+
 #ifdef ANDROID
 static nsCString GetDeviceModelId() {
   // Assumed to be running on the main thread
@@ -882,8 +888,6 @@ nsresult nsHttpHandler::GetIOService(nsIIOService** result) {
 }
 
 void nsHttpHandler::NotifyObservers(nsIChannel* chan, const char* event) {
-  LOG(("nsHttpHandler::NotifyObservers [this=%p chan=%p event=\"%s\"]\n", this,
-       chan, event));
   nsCOMPtr<nsIObserverService> obsService = services::GetObserverService();
   if (obsService) obsService->NotifyObservers(chan, event, nullptr);
 }
@@ -2196,14 +2200,21 @@ nsHttpHandler::NewProxiedChannel(nsIURI* uri, nsIProxyInfo* givenProxyInfo,
 
   if (IsNeckoChild()) {
     httpChannel = new HttpChannelChild();
+    
   } else {
     // HACK: make sure PSM gets initialized on the main thread.
     net_EnsurePSMInit();
     httpChannel = new nsHttpChannel();
   }
 
-  return SetupChannelInternal(httpChannel, uri, givenProxyInfo,
+  nsresult setup_result = SetupChannelInternal(httpChannel, uri, givenProxyInfo,
                               proxyResolveFlags, proxyURI, aLoadInfo, result);
+  if(NS_FAILED(setup_result)){
+    printf("SetupChannelInternal failed\n");
+    return setup_result;
+  }
+
+  return setup_result;
 }
 
 nsresult nsHttpHandler::CreateTRRServiceChannel(
@@ -2571,6 +2582,24 @@ nsHttpHandler::SpeculativeConnect(nsIURI* aURI, nsIPrincipal* aPrincipal,
                                     aAnonymous);
 }
 
+NS_IMETHODIMP
+nsHttpHandler::AddCredential(uint64_t aBcID, const nsACString& aNonce,
+                             const nsACString& aActualCredential,
+                             const nsTArray<nsCString>& aMethods,
+                             const nsACString& aOrigin) {
+  printf("[nsHttpHandler] AddCredential: bcID=%" PRIu64 " nonce=%s origin=%s\n",
+         aBcID, PromiseFlatCString(aNonce).get(),
+         PromiseFlatCString(aOrigin).get());
+  Credential cred;
+  cred.nonce = aNonce;
+  cred.actualCredential = aActualCredential;
+  cred.methods = aMethods.Clone();
+  cred.origin = aOrigin;
+  mCredMap.InsertOrUpdate(aBcID, std::move(cred));
+  printf("[nsHttpHandler] AddCredential: map size after insert=%u\n", mCredMap.Count());
+  return NS_OK;
+}
+
 NS_IMETHODIMP nsHttpHandler::SpeculativeConnectWithOriginAttributes(
     nsIURI* aURI, JS::Handle<JS::Value> aOriginAttributes,
     nsIInterfaceRequestor* aCallbacks, bool aAnonymous, JSContext* aCx) {
@@ -3101,4 +3130,140 @@ void nsHttpHandler::ObserveHttpActivityWithArgs(
       aExtraStringData);
 }
 
+Credential nsHttpHandler::GetCredential(uint64_t aBcID,
+                                         const nsACString& aUrl) {
+  auto entry = mCredMap.Lookup(aBcID);
+  if (entry) {
+    const Credential& data = entry.Data();
+    printf("[nsHttpHandler] GetCredential: bcID=%" PRIu64 " url=%s found nonce=%s\n",
+           aBcID, PromiseFlatCString(aUrl).get(), data.nonce.get());
+    Credential copy;
+    copy.nonce = data.nonce;
+    copy.actualCredential = data.actualCredential;
+    copy.methods = data.methods.Clone();
+    copy.origin = data.origin;
+    return copy;
+  }
+  printf("[nsHttpHandler] GetCredential: bcID=%" PRIu64 " url=%s not found\n",
+         aBcID, PromiseFlatCString(aUrl).get());
+  return Credential();
+}
+
+void nsHttpHandler::RemoveCredential(uint64_t aBcID) {
+  printf("[nsHttpHandler] RemoveCredential: bcID=%" PRIu64 " map size before=%zu\n",
+         aBcID, mCredMap.Count());
+  mCredMap.Remove(aBcID);
+}
+
+nsresult nsHttpHandler::ReplaceNonce(nsIHttpChannel* chan, const Credential& cred,
+                                     uint64_t aBcID) {
+  nsCOMPtr<nsIURI> uri;
+  nsresult rv = chan->GetURI(getter_AddRefs(uri));
+  if (NS_FAILED(rv) || !uri) {
+    return NS_ERROR_FAILURE;
+  }
+
+  nsAutoCString uriSpec;
+  uri->GetSpec(uriSpec);
+  printf("[nsHttpHandler] ReplaceNonce: bcID=%" PRIu64 " url=%s nonce=%s\n",
+         aBcID, uriSpec.get(), cred.nonce.get());
+
+  if (!uri->SchemeIs("https")) {
+    printf("[nsHttpHandler] ReplaceNonce: not HTTPS, skipping\n");
+    // return NS_OK;
+  }
+
+  nsAutoCString scheme;
+  nsAutoCString host;
+  int32_t port = -1;
+  uri->GetScheme(scheme);
+  uri->GetAsciiHost(host);
+  uri->GetPort(&port);
+  nsAutoCString requestOrigin;
+  requestOrigin.Assign(scheme);
+  requestOrigin.AppendLiteral("://");
+  requestOrigin.Append(host);
+  if (port != -1) {
+    requestOrigin.Append(':');
+    requestOrigin.AppendInt(port);
+  }
+
+  if (!cred.origin.Equals(requestOrigin)) {
+    printf("[nsHttpHandler] ReplaceNonce: origin mismatch (%s vs %s), skipping\n",
+           cred.origin.get(), requestOrigin.get());
+    return NS_OK;
+  }
+
+  if (!cred.methods.IsEmpty()) {
+    nsAutoCString method;
+    nsresult rv = chan->GetRequestMethod(method);
+    if (NS_FAILED(rv)) {
+      return rv;
+    }
+    printf("[nsHttpHandler] ReplaceNonce: request method=%s\n", method.get());
+    if (!cred.methods.Contains(method)) {
+      printf("[nsHttpHandler] ReplaceNonce: method not in policy, skipping\n");
+      return NS_OK;
+    }
+  }
+
+  nsCOMPtr<nsIUploadChannel> uploadChannel = do_QueryInterface(chan);
+  nsCOMPtr<nsIInputStream> checkStream;
+  if (!uploadChannel ||
+      NS_FAILED(uploadChannel->GetUploadStream(getter_AddRefs(checkStream))) ||
+      !checkStream) {
+    printf("[nsHttpHandler] ReplaceNonce: no upload stream, skipping\n");
+    return NS_OK;
+  }
+
+  nsAutoCString contentType;
+  rv = chan->GetRequestHeader("Content-Type"_ns, contentType);
+  if (NS_FAILED(rv)) {
+    contentType.Truncate();
+  }
+
+  nsCOMPtr<nsIInputStream> stream;
+  uploadChannel->GetUploadStream(getter_AddRefs(stream));
+  nsCString body;
+  rv = NS_ReadInputStreamToString(stream, body, -1);
+  if (NS_FAILED(rv)) {
+    return rv;
+  }
+
+  if (body.Find(cred.nonce) < 0) {
+    printf("[nsHttpHandler] ReplaceNonce: nonce not found in body\n");
+    return NS_OK;
+  }
+
+  printf("[nsHttpHandler] ReplaceNonce: nonce found in body, replacing\n");
+  body.ReplaceSubstring(cred.nonce, cred.actualCredential);
+  RemoveCredential(aBcID);
+
+  nsCOMPtr<nsIInputStream> newStream;
+  rv = NS_NewByteInputStream(getter_AddRefs(newStream), body, NS_ASSIGNMENT_COPY);
+  if (NS_FAILED(rv)) {
+    return rv;
+  }
+
+  nsCOMPtr<nsIUploadChannel2> uploadChannel2 = do_QueryInterface(chan);
+  if (uploadChannel2) {
+    nsAutoCString method;
+    rv = chan->GetRequestMethod(method);
+    if (NS_FAILED(rv)) {
+      return rv;
+    }
+    rv = uploadChannel2->ExplicitSetUploadStream(newStream, contentType,
+                                                  body.Length(), method, false);
+  } else {
+    rv = uploadChannel->SetUploadStream(newStream, contentType, body.Length());
+  }
+  if (NS_FAILED(rv)) {
+    return rv;
+  }
+
+  (void)chan->SetRequestHeader("Content-Length"_ns,
+                               nsPrintfCString("%zu", body.Length()), false);
+  return NS_OK;
+}
+
 }  // namespace mozilla::net
diff --git a/netwerk/protocol/http/nsHttpHandler.h b/netwerk/protocol/http/nsHttpHandler.h
index bb82c46edf86..f73ecb9ac877 100644
--- a/netwerk/protocol/http/nsHttpHandler.h
+++ b/netwerk/protocol/http/nsHttpHandler.h
@@ -29,6 +29,7 @@
 #include "nsTHashMap.h"
 #include "nsTHashSet.h"
 
+
 #ifdef DEBUG
 #  include "nsIOService.h"
 #endif
@@ -99,6 +100,16 @@ enum FrameCheckLevel {
   FRAMECHECK_STRICT
 };
 
+struct Credential {
+  nsCString nonce;
+  nsCString actualCredential;
+  nsTArray<nsCString> methods;
+  nsCString origin;
+  bool IsEmpty() const {
+    return nonce.IsEmpty() || actualCredential.IsEmpty();
+  }
+};
+
 //-----------------------------------------------------------------------------
 // nsHttpHandler - protocol handler for HTTP and HTTPS
 //-----------------------------------------------------------------------------
@@ -117,6 +128,11 @@ class nsHttpHandler final : public nsIHttpProtocolHandler,
 
   static already_AddRefed<nsHttpHandler> GetInstance();
 
+  Credential GetCredential(uint64_t aBcID, const nsACString& aUrl);
+  void RemoveCredential(uint64_t aBcID);
+  nsresult ReplaceNonce(nsIHttpChannel* chan, const Credential& cred,
+                        uint64_t aBcID);
+
   [[nodiscard]] nsresult AddAcceptAndDictionaryHeaders(
       nsIURI* aURI, ExtContentPolicyType aType, nsHttpRequestHead* aRequest,
       bool aSecure, nsHttpChannel* aChan, void (*aSuspend)(nsHttpChannel*),
@@ -523,6 +539,8 @@ void OnRequestCredentials(nsIHttpChannel* chan) {
 
   [[nodiscard]] nsresult Init();
 
+  nsTHashMap<nsUint64HashKey, Credential> mCredMap;
+
   //
   // Useragent/prefs helper methods
   //
diff --git a/netwerk/protocol/http/nsIHttpProtocolHandler.idl b/netwerk/protocol/http/nsIHttpProtocolHandler.idl
index 06b9b31bff69..ace13ffe724d 100644
--- a/netwerk/protocol/http/nsIHttpProtocolHandler.idl
+++ b/netwerk/protocol/http/nsIHttpProtocolHandler.idl
@@ -91,6 +91,17 @@ interface nsIHttpProtocolHandler : nsIProxiedProtocolHandler
      * Clears the CORS preflight cache.
      */
     void clearCORSPreflightCache();
+
+    /**
+     * Register a nonce-to-credential mapping for a browsing context.
+     * The nonce in the request body will be replaced with the actual
+     * credential before the request is sent over the network.
+     */
+    void addCredential(in unsigned long long bcID,
+                       in ACString nonce,
+                       in ACString actualCredential,
+                       in Array<ACString> methods,
+                       in ACString origin);
 };
 
 %{C++
diff --git a/passwords_secret.pdf b/passwords_secret.pdf
new file mode 100644
index 000000000000..6a6eac7e9b4c
Binary files /dev/null and b/passwords_secret.pdf differ
diff --git a/replace.svg b/replace.svg
new file mode 100644
index 000000000000..d6ce604ba969
--- /dev/null
+++ b/replace.svg
@@ -0,0 +1,77 @@
+<svg xmlns="http://www.w3.org/2000/svg" width="860" height="600" font-family="'Courier New', monospace" font-size="13">
+  <!-- Background -->
+  <rect width="860" height="600" fill="#0f1117"/>
+
+  <!-- Title -->
+  <text x="430" y="32" text-anchor="middle" fill="#e2e8f0" font-size="15" font-weight="bold" letter-spacing="1">Web Request Nonce Replacement Sequence</text>
+
+  <!-- Browser group box -->
+  <rect x="368" y="38" width="468" height="522" rx="6" fill="none" stroke="#475569" stroke-width="1.5" stroke-dasharray="7,4"/>
+  <text x="602" y="572" text-anchor="middle" fill="#475569" font-size="11" letter-spacing="1">BROWSER</text>
+
+  <!-- Dividing dotted line between Handler and Stack -->
+  <line x1="580" y1="50" x2="580" y2="560" stroke="#334155" stroke-width="1" stroke-dasharray="4,4"/>
+
+  <!-- Lifeline headers -->
+  <!-- User -->
+  <rect x="30" y="50" width="100" height="34" rx="4" fill="#1e293b" stroke="#38bdf8" stroke-width="1.5"/>
+  <text x="80" y="72" text-anchor="middle" fill="#38bdf8" font-weight="bold">User</text>
+
+  <!-- Extension -->
+  <rect x="190" y="50" width="120" height="34" rx="4" fill="#1e293b" stroke="#a78bfa" stroke-width="1.5"/>
+  <text x="250" y="72" text-anchor="middle" fill="#a78bfa" font-weight="bold">Extension</text>
+
+  <!-- WebRequest Handler -->
+  <rect x="378" y="50" width="160" height="34" rx="4" fill="#1e293b" stroke="#34d399" stroke-width="1.5"/>
+  <text x="458" y="72" text-anchor="middle" fill="#34d399" font-weight="bold">WebRequest Handler</text>
+
+  <!-- Network Stack -->
+  <rect x="620" y="50" width="130" height="34" rx="4" fill="#1e293b" stroke="#fb923c" stroke-width="1.5"/>
+  <text x="685" y="72" text-anchor="middle" fill="#fb923c" font-weight="bold">Network Stack</text>
+
+  <!-- Lifelines -->
+  <line x1="80"  y1="84" x2="80"  y2="560" stroke="#38bdf8" stroke-width="1" stroke-dasharray="5,4" opacity="0.4"/>
+  <line x1="250" y1="84" x2="250" y2="560" stroke="#a78bfa" stroke-width="1" stroke-dasharray="5,4" opacity="0.4"/>
+  <line x1="458" y1="84" x2="458" y2="560" stroke="#34d399" stroke-width="1" stroke-dasharray="5,4" opacity="0.4"/>
+  <line x1="685" y1="84" x2="685" y2="560" stroke="#fb923c" stroke-width="1" stroke-dasharray="5,4" opacity="0.4"/>
+
+  <defs>
+    <marker id="arr" markerWidth="8" markerHeight="8" refX="7" refY="3" orient="auto">
+      <path d="M0,0 L0,6 L8,3 z" fill="#94a3b8"/>
+    </marker>
+    <marker id="arr-ret" markerWidth="8" markerHeight="8" refX="7" refY="3" orient="auto">
+      <path d="M0,0 L0,6 L8,3 z" fill="#64748b"/>
+    </marker>
+    <marker id="arr-self" markerWidth="8" markerHeight="8" refX="7" refY="3" orient="auto">
+      <path d="M0,0 L0,6 L8,3 z" fill="#fb923c"/>
+    </marker>
+  </defs>
+
+  <!-- Step 1: User initiates web request → Network Stack -->
+  <line x1="80" y1="140" x2="678" y2="140" stroke="#94a3b8" stroke-width="1.5" marker-end="url(#arr)"/>
+  <text x="378" y="133" text-anchor="middle" fill="#94a3b8" font-size="11">initiates web request</text>
+  <text x="10" y="143" fill="#475569" font-size="10">[1]</text>
+
+  <!-- Step 2: Network Stack creates request (internal) -->
+  <rect x="610" y="168" width="150" height="26" rx="3" fill="#1c1a14" stroke="#fb923c" stroke-width="1" stroke-dasharray="4,3"/>
+  <text x="685" y="185" text-anchor="middle" fill="#fb923c" font-size="11">creates request</text>
+  <text x="10" y="183" fill="#475569" font-size="10">[2]</text>
+
+  <!-- Step 3: Network Stack sends onBeforeRequest → WebRequest Handler -->
+  <line x1="685" y1="230" x2="465" y2="230" stroke="#94a3b8" stroke-width="1.5" marker-end="url(#arr)"/>
+  <text x="575" y="222" text-anchor="middle" fill="#94a3b8" font-size="11">onBeforeRequest</text>
+  <text x="10" y="233" fill="#475569" font-size="10">[3]</text>
+
+  <!-- Step 4: Network Stack checks map (internal note) -->
+  <rect x="610" y="258" width="150" height="26" rx="3" fill="#1c1a14" stroke="#fb923c" stroke-width="1" stroke-dasharray="4,3"/>
+  <text x="685" y="275" text-anchor="middle" fill="#fb923c" font-size="11">check map for URL entry</text>
+  <text x="10" y="273" fill="#475569" font-size="10">[4]</text>
+
+  <!-- Step 5: Network Stack self-call replaceHandler() -->
+  <line x1="685" y1="345" x2="735" y2="345" stroke="#fb923c" stroke-width="1.5"/>
+  <line x1="735" y1="345" x2="735" y2="380" stroke="#fb923c" stroke-width="1.5"/>
+  <line x1="735" y1="380" x2="692" y2="380" stroke="#fb923c" stroke-width="1.5" marker-end="url(#arr-self)"/>
+  <text x="676" y="338" text-anchor="middle" fill="#fb923c" font-size="11">replaceNonce(nonce)</text>
+  <text x="10" y="362" fill="#475569" font-size="10">[5]</text>
+
+</svg>
diff --git a/scratch.svg b/scratch.svg
new file mode 100644
index 000000000000..28b3d312e38b
--- /dev/null
+++ b/scratch.svg
@@ -0,0 +1,80 @@
+<svg xmlns="http://www.w3.org/2000/svg" width="820" height="560" font-family="'Courier New', monospace" font-size="13">
+  <!-- Background -->
+  <rect width="820" height="500" fill="#0f1117"/>
+
+  <!-- Title -->
+  <text x="410" y="32" text-anchor="middle" fill="#e2e8f0" font-size="15" font-weight="bold" letter-spacing="1">Nonce Replacement Sequence</text>
+
+  <!-- Browser group box (behind lifeline headers) -->
+  <rect x="408" y="38" width="398" height="410" rx="6" fill="none" stroke="#475569" stroke-width="1.5" stroke-dasharray="7,4"/>
+  <text x="607" y="465" text-anchor="middle" fill="#475569" font-size="11" letter-spacing="1">BROWSER</text>
+
+  <!-- Dividing dotted line between Handler and Stack -->
+
+  <!-- Lifeline headers -->
+  <!-- User -->
+  <rect x="30" y="50" width="110" height="34" rx="4" fill="#1e293b" stroke="#38bdf8" stroke-width="1.5"/>
+  <text x="85" y="72" text-anchor="middle" fill="#38bdf8" font-weight="bold">User</text>
+
+  <!-- Extension -->
+  <rect x="220" y="50" width="130" height="34" rx="4" fill="#1e293b" stroke="#a78bfa" stroke-width="1.5"/>
+  <text x="285" y="72" text-anchor="middle" fill="#a78bfa" font-weight="bold">Extension</text>
+
+  <!-- WebRequest Handler -->
+  <rect x="420" y="50" width="170" height="34" rx="4" fill="#1e293b" stroke="#34d399" stroke-width="1.5"/>
+  <text x="505" y="72" text-anchor="middle" fill="#34d399" font-weight="bold">WebRequest Handler</text>
+
+  <!-- Network Stack -->
+  <rect x="660" y="50" width="130" height="34" rx="4" fill="#1e293b" stroke="#fb923c" stroke-width="1.5"/>
+  <text x="725" y="72" text-anchor="middle" fill="#fb923c" font-weight="bold">Network Stack</text>
+
+  <!-- Lifelines (dashed vertical) -->
+  <line x1="85"  y1="84" x2="85"  y2="530" stroke="#38bdf8" stroke-width="1" stroke-dasharray="5,4" opacity="0.4"/>
+  <line x1="285" y1="84" x2="285" y2="530" stroke="#a78bfa" stroke-width="1" stroke-dasharray="5,4" opacity="0.4"/>
+  <line x1="505" y1="84" x2="505" y2="530" stroke="#34d399" stroke-width="1" stroke-dasharray="5,4" opacity="0.4"/>
+  <line x1="725" y1="84" x2="725" y2="530" stroke="#fb923c" stroke-width="1" stroke-dasharray="5,4" opacity="0.4"/>
+
+  <!-- Helper: arrow marker -->
+  <defs>
+    <marker id="arr" markerWidth="8" markerHeight="8" refX="7" refY="3" orient="auto">
+      <path d="M0,0 L0,6 L8,3 z" fill="#94a3b8"/>
+    </marker>
+    <marker id="arr-ret" markerWidth="8" markerHeight="8" refX="7" refY="3" orient="auto">
+      <path d="M0,0 L0,6 L8,3 z" fill="#64748b"/>
+    </marker>
+    <marker id="arr-self" markerWidth="8" markerHeight="8" refX="7" refY="3" orient="auto">
+      <path d="M0,0 L0,6 L8,3 z" fill="#fb923c"/>
+    </marker>
+  </defs>
+
+    <!-- Step 1: User initiates web request → Network Stack -->
+  <line x1="85" y1="140" x2="725" y2="140" stroke="#94a3b8" stroke-width="1.5" marker-end="url(#arr)"/>
+  <text x="378" y="133" text-anchor="middle" fill="#94a3b8" font-size="11">user initiates login request</text>
+  <text x="260" y="133" fill="#475569" font-size="10">[1]</text>
+
+  <!-- Step 2: Network Stack creates request (internal) -->
+  <rect x="660" y="168" width="125" height="26" rx="3" fill="#1c1a14" stroke="#fb923c" stroke-width="1" stroke-dasharray="4,3"/>
+  <text x="725" y="185" text-anchor="middle" fill="#fb923c" font-size="11">creates request</text>
+  <text x="630" y="183" fill="#475569" font-size="10">[2]</text>
+
+  <!-- Step 3: Network Stack sends onBeforeRequest → WebRequest Handler -->
+  <line x1="725" y1="230" x2="505" y2="230" stroke="#94a3b8" stroke-width="1.5" marker-end="url(#arr)"/>
+  <text x="575" y="222" text-anchor="middle" fill="#94a3b8" font-size="11">observers are notified of outgoing request</text>
+  <text x="415" y="222" fill="#475569" font-size="10">[3]</text>
+
+  <!-- Step 4: Network Stack checks map (internal note) -->
+  <rect x="653" y="258" width="140" height="26" rx="3" fill="#1c1a14" stroke="#fb923c" stroke-width="1" stroke-dasharray="4,3"/>
+  <text x="725" y="275" text-anchor="middle" fill="#fb923c" font-size="11">check map for entry</text>
+  <text x="625" y="273" fill="#475569" font-size="10">[4]</text>
+
+  <!-- Step 5: Network Stack self-call replaceHandler() -->
+  <line x1="725" y1="345" x2="775" y2="345" stroke="#fb923c" stroke-width="1.5"/>
+  <line x1="775" y1="345" x2="775" y2="380" stroke="#fb923c" stroke-width="1.5"/>
+  <line x1="775" y1="380" x2="732" y2="380" stroke="#fb923c" stroke-width="1.5" marker-end="url(#arr-self)"/>
+  <text x="716" y="338" text-anchor="middle" fill="#fb923c" font-size="11">replaceNonce(nonce)</text>
+  <text x="630" y="338" fill="#475569" font-size="10">[5]</text>
+
+  <!-- Step row labels on right -->
+
+
+</svg>
diff --git a/toolkit/components/extensions/parent/ext-webRequest.js b/toolkit/components/extensions/parent/ext-webRequest.js
index f5d4199ead57..5c1a90bceda7 100644
--- a/toolkit/components/extensions/parent/ext-webRequest.js
+++ b/toolkit/components/extensions/parent/ext-webRequest.js
@@ -4,18 +4,12 @@
 
 "use strict";
 
+// eslint-disable-next-line mozilla/reject-importGlobalProperties
+
 ChromeUtils.defineESModuleGetters(this, {
   WebRequest: "resource://gre/modules/WebRequest.sys.mjs",
 });
 
-// Add this debug block temporarily:
-console.log("=== WebRequest Debug ===");
-// console.log("WebRequest object:", WebRequest);
-// console.log("onBeforeRequest exists:", !!WebRequest.onBeforeRequest);
-// console.log("onRequestCredentials exists:", !!WebRequest.onRequestCredentials);
-// console.log("Available WebRequest keys:", Object.keys(WebRequest));
-console.log("========================");
-
 var { parseMatchPatterns, DefaultMap } = ExtensionUtils;
 
 const MAX_HANDLER_BEHAVIOR_CHANGED_CALLS_PER_10_MINUTES = 20;
diff --git a/toolkit/components/extensions/schemas/manifest.json b/toolkit/components/extensions/schemas/manifest.json
index f5ff348b11c8..5dfc68c95446 100644
--- a/toolkit/components/extensions/schemas/manifest.json
+++ b/toolkit/components/extensions/schemas/manifest.json
@@ -554,7 +554,7 @@
           { "$ref": "PermissionPrivileged" },
           {
             "type": "string",
-            "enum": ["alarms", "storage", "unlimitedStorage"]
+            "enum": ["alarms", "secretStorage", "storage", "unlimitedStorage"]
           }
         ]
       },
diff --git a/xpcom/ds/StaticAtoms.py b/xpcom/ds/StaticAtoms.py
index 2547d78336e5..a786da572e0a 100644
--- a/xpcom/ds/StaticAtoms.py
+++ b/xpcom/ds/StaticAtoms.py
@@ -2044,6 +2044,7 @@ STATIC_ATOMS = [
     Atom("debugger", "debugger"),
     Atom("mozillaAddons", "mozillaAddons"),
     Atom("tabs", "tabs"),
+    Atom("secretStorage", "secretStorage"),
     Atom("webRequestBlocking", "webRequestBlocking"),
     Atom("webRequestFilterResponse_serviceWorkerScript", "webRequestFilterResponse.serviceWorkerScript"),
     Atom("blob", "blob"),
